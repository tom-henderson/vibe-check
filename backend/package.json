{
  "name": "current-mood-backend-workspace",
  "version": "1.0.0",
  "description": "Dev/test workspace for the Current Mood Lambda (deployed code lives in src/)",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node test/handler.test.mjs"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.1106.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "aws-sdk-client-mock": "^4.1.0"
  }
}
